[tool.pytest.ini_options]
testpaths = ["scripts"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Проверяем ошибки исполнения, не навязываем проекту стиль форматирования.
select = ["F"]
