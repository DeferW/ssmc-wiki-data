diff --git a/Content.MapRenderer/MapViewerData.cs b/Content.MapRenderer/MapViewerData.cs
index b58d0c6..7125a5a 100644
--- a/Content.MapRenderer/MapViewerData.cs
+++ b/Content.MapRenderer/MapViewerData.cs
@@ -20,6 +20,7 @@ public sealed class GridLayer
 {
     public string GridId { get; set; } = string.Empty;
     public Position Offset { get; set; }
+    public Position WorldMin { get; set; }
     public bool Tiled { get; set; } = false;
     public string Url { get; set; }
     public Extent Extent { get; set; }
@@ -31,6 +32,7 @@ public GridLayer(RenderedGridImage<Rgba32> gridImage, string url)
             GridId = gridImage.GridUid.Value.GetHashCode().ToString();
 
         Offset = new Position(gridImage.Offset);
+        WorldMin = new Position(gridImage.WorldMin);
         Extent = new Extent(gridImage.Image.Width, gridImage.Image.Height);
         Url = url;
     }
diff --git a/Content.MapRenderer/Painters/MapPainter.cs b/Content.MapRenderer/Painters/MapPainter.cs
index 991fa74..ff5673a 100644
--- a/Content.MapRenderer/Painters/MapPainter.cs
+++ b/Content.MapRenderer/Painters/MapPainter.cs
@@ -47,9 +47,10 @@ namespace Content.MapRenderer.Painters
             var poolSettings = new PoolSettings
             {
                 DummyTicker = false,
                 Connected = true,
-                Destructive = true,
-                Fresh = true,
+                Destructive = false,
+                Fresh = false,
+                Dirty = true,
                 // Seriously whoever made MapPainter use GameMapPrototype I wish you step on a lego one time.
                 Map = _map is RenderMapPrototype prototype ? prototype.Prototype : PoolManager.TestMap,
             };
@@ -204,11 +204,15 @@ await server.WaitPost(() =>
                 var maxY = tiles.Max(t => t.Y);
                 var w = (maxX - minX + 1) * tileXSize;
                 var h = (maxY - minY + 1) * tileYSize;
+                var worldMin = new Vector2(grid.LocalAABB.Left, grid.LocalAABB.Bottom);
                 var customOffset = new Vector2();
 
                 //MapGrids don't have LocalAABB, so we offset them to align the bottom left corner with 0,0 coordinates
                 if (grid.LocalAABB.IsEmpty())
+                {
                     customOffset = new Vector2(-minX, -minY);
+                    worldMin = new Vector2(minX * grid.TileSize, minY * grid.TileSize);
+                }
 
                 var gridCanvas = new Image<Rgba32>(w, h);
 
@@ -224,6 +228,7 @@ await server.WaitPost(() =>
                 {
                     GridUid = uid,
                     Offset = xformSystem.GetWorldPosition(uid),
+                    WorldMin = worldMin,
                 };
 
                 yield return renderedImage;
diff --git a/Content.MapRenderer/RenderedGridImage.cs b/Content.MapRenderer/RenderedGridImage.cs
index c460853..4f02eaf 100644
--- a/Content.MapRenderer/RenderedGridImage.cs
+++ b/Content.MapRenderer/RenderedGridImage.cs
@@ -10,6 +10,7 @@ public sealed class RenderedGridImage<T> where T : unmanaged, IPixel<T>
 {
     public Image<T> Image;
     public Vector2 Offset { get; set; } = Vector2.Zero;
+    public Vector2 WorldMin { get; set; } = Vector2.Zero;
     public EntityUid? GridUid { get; set; }
 
     public RenderedGridImage(Image<T> image)
diff --git a/Content.MapRenderer/Painters/EntityPainter.cs b/Content.MapRenderer/Painters/EntityPainter.cs
--- a/Content.MapRenderer/Painters/EntityPainter.cs
+++ b/Content.MapRenderer/Painters/EntityPainter.cs
@@ -129,5 +129,5 @@ public void Run(Image canvas, EntityData entity, SharedTransformSystem xformSyst
-            var spriteRotation = 0f;
+            var spriteRotation = (float) entity.Sprite.Rotation.Degrees;
             if (!entity.Sprite.NoRotation && !entity.Sprite.SnapCardinals && _sprite.LayerGetDirectionCount((SpriteComponent.Layer)layer) == 1)
             {
-                spriteRotation = (float)worldRotation.Degrees;
+                spriteRotation += (float)worldRotation.Degrees;
             }
@@ -140,8 +140,14 @@ public void Run(Image canvas, EntityData entity, SharedTransformSystem xformSyst
             var (imgX, imgY) = rsi?.Size ?? (EyeManager.PixelsPerMeter, EyeManager.PixelsPerMeter);
-            var offsetX = (int)(entity.Sprite.Offset.X + customOffset.X) * EyeManager.PixelsPerMeter;
-            var offsetY = (int)(entity.Sprite.Offset.Y + customOffset.X) * EyeManager.PixelsPerMeter;
+            var transformRotation = entity.Sprite.NoRotation || entity.Sprite.SnapCardinals
+                ? worldRotation - worldRotation
+                : worldRotation;
+            var rotatedOffset = Vector2.Transform(
+                entity.Sprite.Offset,
+                Matrix3x2.CreateRotation((float) (entity.Sprite.Rotation + transformRotation).Theta));
+            var offsetX = (int) MathF.Round((rotatedOffset.X + customOffset.X) * EyeManager.PixelsPerMeter);
+            var offsetY = (int) MathF.Round((rotatedOffset.Y + customOffset.Y) * EyeManager.PixelsPerMeter);
             image.Mutate(o => o
                 .DrawImage(coloredImage, PixelColorBlendingMode.Multiply, PixelAlphaCompositionMode.SrcAtop, 1)
                 .Resize(imgX, imgY)
                 .Flip(FlipMode.Vertical)
                 .Rotate(spriteRotation));
@@ -149,4 +155,4 @@ public void Run(Image canvas, EntityData entity, SharedTransformSystem xformSyst
-            var pointX = (int)entity.X + offsetX - imgX / 2;
-            var pointY = (int)entity.Y + offsetY - imgY / 2;
+            var pointX = (int) MathF.Round(entity.X) + offsetX - image.Width / 2;
+            var pointY = (int) MathF.Round(entity.Y) + offsetY - image.Height / 2;
             canvas.Mutate(o => o.DrawImage(image, new Point(pointX, pointY), 1));
         }
     }
diff --git a/Content.MapRenderer/Program.cs b/Content.MapRenderer/Program.cs
index 9d7843bcd0..729ab65706 100644
--- a/Content.MapRenderer/Program.cs
+++ b/Content.MapRenderer/Program.cs
@@ -222,7 +222,10 @@ private static async Task Run(
                             case OutputFormat.webp:
                                 var encoder = new WebpEncoder
                                 {
-                                    Method = WebpEncodingMethod.BestQuality,
+                                    // This full image is an intermediate input for the tile packager.
+                                    // Lossless preserves identical pixels; fastest avoids spending minutes
+                                    // optimizing a file that will immediately be decoded again.
+                                    Method = WebpEncodingMethod.Fastest,
                                     FileFormat = WebpFileFormatType.Lossless,
                                     TransparentColorMode = WebpTransparentColorMode.Preserve
                                 };
